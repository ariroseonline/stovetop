//takes in 2 things, the action and a copy of current state

export default function posts (state = [], action) {
  // switch(action.type) {
  //   case "ADD POST":
  //     return state;
  // }
  return state;
}